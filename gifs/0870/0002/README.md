# Falinks GIF previews

One native-size looping GIF per AnimData entry, direction 0 (down/front). Durations use the PMD 60 Hz clock; identical consecutive poses are coalesced without changing total time. The authoritative PMD sheets remain in `sprite/0870/0002/`.
