const fs=require('fs'),vm=require('vm'),assert=require('assert');let raf,draws=0;const els={};class E{constructor(){this.children=[];this.complete=true;this.naturalWidth=1264}append(...v){this.children.push(...v)}getContext(){return{clearRect(){draws=0},drawImage(){draws++}}}click(){}toDataURL(){return'data:image/png;base64,x'}}const doc={getElementById(id){return els[id]??=new E()},createElement(){return new E()},createTextNode(t){return t}};const context={document:doc,Image:E,requestAnimationFrame(f){raf=f}};vm.createContext(context);vm.runInContext(fs.readFileSync('apercu_cafe_calques_v3.html','utf8').match(/<script>([\s\S]*?)<\/script>/)[1],context);for(const [mode,count]of [['vide',1],['spirales',2],['casse',4],['meublee',8]]){vm.runInContext(`preset('${mode}')`,context);raf();assert.equal(draws,count)}assert.equal(els.options.children.length,13);console.log('PASS DOM simule:13 calques et4 preselections.');
