const fs=require('fs'),vm=require('vm'),assert=require('assert');let raf,draws=0;const els={};class E{constructor(){this.children=[];this.complete=true;this.naturalWidth=1200}append(...v){this.children.push(...v)}getContext(){return{clearRect(){draws=0},drawImage(){draws++}}}click(){}toDataURL(){return'data:image/png;base64,x'}}const doc={getElementById(id){return els[id]??=new E()},createElement(){return new E()},createTextNode(t){return t}};const ctx={document:doc,Image:E,requestAnimationFrame(f){raf=f}};vm.createContext(ctx);vm.runInContext(fs.readFileSync('apercu_sakura_entree_calques_v1.html','utf8').match(/<script>([\s\S]*?)<\/script>/)[1],ctx);for(const [mode,count] of [['all',7],['floor',1],['trees',3],['shadow',1]]){vm.runInContext(`preset('${mode}')`,ctx);raf();assert.equal(draws,count)}els.atlas.onclick();raf();assert.equal(els.view.width,416);assert.equal(els.view.height,216);assert.equal(els.layers.children.length,7);console.log('PASS DOM simule:7 calques,4 preselections,feuille arbres');
