const fs=require('fs'),vm=require('vm'),assert=require('assert');let raf,draws=0;const els={};class E{constructor(){this.children=[];this.complete=true;this.naturalWidth=400}set innerHTML(v){this.children=[]}get innerHTML(){return''}append(...v){this.children.push(...v)}getContext(){return{clearRect(){draws=0},drawImage(){draws++}}}toDataURL(){return'data:image/png;base64,test'}click(){}}const document={getElementById(n){return els[n]??=new E()},createElement(){return new E()},createTextNode(t){return t}};const context={document,Image:E,requestAnimationFrame(f){raf=f},console};vm.createContext(context);const html=fs.readFileSync('apercu_references_54d3731.html','utf8');vm.runInContext(html.match(/<script>([\s\S]*?)<\/script>/)[1],context);assert.equal(els.scene.children.length,10);for(let i=0;i<10;i++){els.scene.onchange({target:{value:String(i)}});for(const mode of ['jour','nuit']){els.mode.onchange({target:{value:mode}});raf();assert.ok(draws>=3);assert.equal(draws,els.layers.children.length);assert.match(els.status.textContent,/statique/);for(const l of els.layers.children){const ch=l.children[0];ch.checked=false;ch.onchange()}raf();assert.equal(draws,0);els.all.onclick();raf();assert.ok(draws>=3)}}console.log('PASS: 10 scènes × 2 ambiances, contrôles de calques, masquage total et restauration (DOM simulé).');
