# Dernière demande prioritaire

1. L’utilisateur demandait20 biomes originaux, plusieurs layouts guidés par les nouveaux fichiers du dernier commit et les références Sky/Rescue Team, méthode Northern.
2. Les références du commit8eb46bc ont été inspectées et leurs phases0 exportées ; dix planches de deux compositions ont été générées.
3. AVANT finalisation, l’utilisateur a réorienté explicitement : **reproduire les layouts de référence en plusieurs calques, seulement des changements subtils**.
4. Les nouvelles compositions sont donc archivées, pas validées ni présentées comme le livrable.
5. Livraison courante : `renders/references_fideles_v1/`, galerie `apercu_references_fideles_v1.html`.14 zones de référence +1 fond, six accents lumineux optionnels ; aucun déplacement/redimensionnement. Les60 images source (incluant toutes les phases des GIF) et les ORA sont vérifiés, les SHA256 sont comparés aux vrais fichiers du commit8eb46bc. DOM simulé passé. Pas de PMDO/GPU.

Recomposer à(0,0), ne pas déplacer les partitions comme des arbres/rochers complets : les pixels occultés ne sont pas reconstruits. Le layer sombre est un ensemble de détails peints, pas une ombre physique universelle. Le fond nocturne PNG reste statique ; seuls les GIF fournis sont animés.

Source actuelle :15 références uniques, doublon D13P11A exclu. Ne pas attribuer aux IDs MAP_BG des noms de donjons non vérifiés. Les fichiers racine et les travaux précédents sont préservés.
