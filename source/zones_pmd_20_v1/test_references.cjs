const fs=require('fs'),vm=require('vm'),assert=require('assert');let raf,draws=0;const els={};class E{constructor(){this.children=[];this.complete=true;this.naturalWidth=552}set id(v){els[v]=this}set innerHTML(v){this.children=[]}append(...v){this.children.push(...v)}getContext(){return{clearRect(){draws=0},drawImage(){draws++}}}click(){}toDataURL(){return'data:image/png;base64,x'}}const doc={getElementById(id){return els[id]??=new E()},createElement(){return new E()},createTextNode(t){return t}};const ctx={document:doc,Image:E,requestAnimationFrame(f){raf=f}};vm.createContext(ctx);vm.runInContext(fs.readFileSync('apercu_references_fideles_v1.html','utf8').match(/<script>([\s\S]*?)<\/script>/)[1],ctx);const m=JSON.parse(fs.readFileSync('renders/references_fideles_v1/manifest.json'));let t=100;for(const e of m.scenes){els.scene.value=e.id;els.scene.onchange();raf(t+=100);assert.equal(draws,e.layers.length+(e.animation?1:0));assert.equal(els.view.width,e.size[0]);els.none.onclick();raf(t+=100);assert.equal(draws,0);els.all.onclick();raf(t+=100);assert(draws>0);els.reference.onclick();raf(t+=100);assert.equal(draws,1)}console.log('PASS DOM simule:15 references,calques,animation native,reference seule');
